class Solution {
    public boolean isPower(int x, int y) {
        // Special case
        if (x == 1) return y == 1;

        // Divide y by x repeatedly
        while (y % x == 0) {
            y = y / x;
        }

        // If y becomes 1, it's a power
        return y == 1;
    }
}
