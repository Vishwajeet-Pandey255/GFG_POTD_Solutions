class Solution {
    int subsetXORSum(int arr[]) {
        int n = arr.length;
        
        int OR = 0;
        for (int x : arr) {
            OR |= x;
        }
        
        return OR * (1 << (n - 1));
    }
}
