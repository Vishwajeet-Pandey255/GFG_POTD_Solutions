class Solution {
    public int maxSum(int[] arr) {
        int maxScore = 0;
        for (int i = 0; i < arr.length - 1; i++) {
            maxScore = Math.max(maxScore, arr[i] + arr[i + 1]);
        }
        return maxScore;
    }
}
