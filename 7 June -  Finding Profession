class Solution {
    public String profession(int level, int pos) {
        int cnt = Integer.bitCount(pos - 1);

        if (cnt % 2 == 0)
            return "Engineer";
        else
            return "Doctor";
    }
}
